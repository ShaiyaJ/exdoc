[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "exdoc"
version = "1.0"

[project.scripts]
exdoc = "src.main:main"

[tool.setuptools.packages.find]
where = ["."]
